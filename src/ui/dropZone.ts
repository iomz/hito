/**
 * Collapse the drop-zone container UI.
 *
 * NOTE: This is now a no-op. React DropZone component handles collapse/expand
 * based on state.currentDirectory automatically.
 */
export function collapseDropZone(): void {
  // No-op: React DropZone component handles collapse state
}

/**
 * Expand the UI drop-zone container to its non-collapsed state.
 *
 * NOTE: This is now a no-op. React DropZone component handles collapse/expand
 * based on state.currentDirectory automatically.
 */
export function expandDropZone(): void {
  // No-op: React DropZone component handles expand state
}

